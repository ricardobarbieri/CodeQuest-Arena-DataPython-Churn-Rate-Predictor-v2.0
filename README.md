# CodeQuest-Arena-DataPython-Churn-Rate-Predictor-v2.0
Reproducible DS pipeline to predict subscription cancellation (churn). Performs data cleaning, missing value handling, categorical variable encoding, comparative training between Logistic Regression and Random Forest, evaluation using multiple metrics (accuracy, precision, recall, F1-Score, AUC-ROC), and exports the best model using joblib.
